// import { metadata } from "@/app/layout";

export const metadata = {
    title: "About Page",
    description: "This is about page",
};
const AboutPage = () => {
    return (
        <div>
            <h1>About page with headre footer</h1>
        </div>
    );
};

export default AboutPage;